---
interface Props {
  title: string;
  subtitle: string;
  ctaText?: string;
  ctaHref?: string;
  secondaryCtaText?: string;
  secondaryCtaHref?: string;
  compact?: boolean;
  statValue?: string;
  statLabel?: string;
}

const {
  title,
  subtitle,
  ctaText,
  ctaHref = '/contact',
  secondaryCtaText,
  secondaryCtaHref,
  compact = false,
  statValue = '8M+',
  statLabel = 'Happy customers weekly',
} = Astro.props;
---

<section class:list={[
  'relative overflow-hidden',
  compact ? 'py-16 lg:py-20' : 'py-0',
]} style={compact ? "background: linear-gradient(135deg, #006F51 0%, #005a42 40%, #1E3A5F 100%);" : ""}>
  {compact ? (
    <!-- Compact: simple left-aligned text, no split screen -->
    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl">
        <h1 class="text-display font-extrabold text-white leading-tight text-3xl sm:text-4xl lg:text-5xl">
          {title}
        </h1>
        <p class="mt-6 text-white/85 max-w-2xl leading-relaxed text-lg">
          {subtitle}
        </p>
      </div>
    </div>
  ) : (
    <!-- Full hero: asymmetric split-screen -->
    <div class="flex flex-col lg:flex-row min-h-[540px] lg:min-h-[600px]">
      <!-- Left: text content — 65% -->
      <div class="lg:w-[65%] flex items-center" style="background: linear-gradient(135deg, #006F51 0%, #005a42 40%, #1E3A5F 100%);">
        <div class="max-w-3xl px-6 sm:px-10 lg:px-16 xl:px-24 py-20 lg:py-28">
          <h1 class="text-display font-extrabold text-white leading-tight">
            {title}
          </h1>
          <p class="mt-6 text-white/80 max-w-xl leading-relaxed text-lg sm:text-xl">
            {subtitle}
          </p>
          {(ctaText || secondaryCtaText) && (
            <div class="mt-10 flex flex-col sm:flex-row items-start gap-4">
              {ctaText && (
                <a
                  href={ctaHref}
                  class="inline-flex items-center gap-2 bg-white text-primary px-8 py-4 rounded-none text-base font-semibold hover:bg-gray-100 transition-colors"
                >
                  {ctaText}
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                  </svg>
                </a>
              )}
              {secondaryCtaText && (
                <a
                  href={secondaryCtaHref}
                  class="inline-flex items-center gap-2 border-2 border-white/30 text-white px-8 py-4 rounded-none text-base font-semibold hover:bg-white/10 transition-colors"
                >
                  {secondaryCtaText}
                </a>
              )}
            </div>
          )}
        </div>
      </div>

      <!-- Right: bold green block with stat — 35% -->
      <div class="lg:w-[35%] bg-primary flex items-center justify-center relative overflow-hidden" style="background-color: #006F51;">
        <!-- Large faded WC monogram in background -->
        <div class="absolute inset-0 flex items-center justify-center opacity-[0.07] select-none pointer-events-none" aria-hidden="true">
          <span class="text-white font-extrabold" style="font-size: 20rem; font-family: var(--font-heading); line-height: 1;">WC</span>
        </div>
        <div class="relative text-center px-8 py-16 lg:py-0">
          <span class="block text-white font-extrabold tracking-tight tabular-nums" style="font-size: clamp(5rem, 10vw, 8rem); font-family: var(--font-heading); line-height: 1;">{statValue}</span>
          <span class="block text-white/70 text-sm font-medium uppercase tracking-widest mt-4">{statLabel}</span>
        </div>
      </div>
    </div>
  )}
</section>
